---
name: fusion-palette-web-ui
description: Build and test Fusion palette web UIs, local companion apps, and browser-verifiable UI surfaces.
---

# Fusion palette web UI

Use for Fusion palette and local web workflows.

## Rules

- Keep palette UI separate from model mutation logic.
- Test DOM interactions in browser tools when possible.
- Use Fusion API evidence for model truth and browser evidence for UI truth.
